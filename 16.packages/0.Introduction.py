# Package is a folder(directory) which contains modules(file.py) and sub-packages
# Package can be accessed by following way follwoing syntax:
# import sys
# sys.path.append("path of the package")
# For Examples and all: refer Python:Packages folders in another project
